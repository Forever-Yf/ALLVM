// Compatibility translation unit retained for downstream scripts that still
// reference the first authenticated-string source name. The build uses the V2
// file directly so this wrapper must not be added to the same target.
#include "StringEncryptionAuthenticatedV2.cpp"
