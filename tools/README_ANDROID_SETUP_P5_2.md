# Android setup planner

Example:

```bash
python3 tools/allvm_android_command.py . --json
```

The planner is read-only. It detects project structure and prepares the action plan for the future `allvm android setup` command.
