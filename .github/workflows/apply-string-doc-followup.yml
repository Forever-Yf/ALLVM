name: Apply string hardening documentation

on:
  push:
    branches:
      - hardening/p0-secure-seeding-doctor
    paths:
      - .github/workflows/apply-string-doc-followup.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply exact documentation edits
        run: python3 tools/apply-string-doc-followup.py

      - name: Check formatting
        run: git diff --check

      - name: Remove one-time helpers
        run: |
          rm tools/apply-string-doc-followup.py
          rm .github/workflows/apply-string-doc-followup.yml

      - name: Commit documentation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            README.md \
            docs/ALLVM_HARDENING.md \
            tools/apply-string-doc-followup.py \
            .github/workflows/apply-string-doc-followup.yml
          git commit -m "document string random lifecycle and limits"
          git push origin "HEAD:${GITHUB_REF_NAME}"
